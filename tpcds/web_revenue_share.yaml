name: ${prefix}web_revenue_share
node_type: metric
display_name: Web Revenue Share
description: Web channel share of total revenue across all channels (percentage)
query: |
  SELECT ${prefix}web_revenue / (${prefix}store_revenue + ${prefix}web_revenue + ${prefix}catalog_revenue) * 100
unit: percentage
