name: ${prefix}catalog_revenue_share
node_type: metric
display_name: Catalog Revenue Share
description: Catalog channel share of total revenue across all channels (percentage)
query: |
  SELECT ${prefix}catalog_revenue / (${prefix}store_revenue + ${prefix}web_revenue + ${prefix}catalog_revenue) * 100
unit: percentage
